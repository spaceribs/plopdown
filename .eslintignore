node_modules
**/*.prod.ts

# Generated by ts-json-schema-generator + ajv; see libs/plopdown-file/src/schema/README.md.
# Nx 17 dropped lintFilePatterns from the eslint executor, so .js files in a project
# are now linted by default and this compiled validator is one huge minified line.
libs/plopdown-file/src/schema/*.js
